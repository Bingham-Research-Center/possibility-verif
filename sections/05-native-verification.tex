% !TEX root = ../main.tex
%----------------------------------------------------------
% 05-native-verification.tex — Section 4: Native Possibilistic Verification
% MIGRATED from preprint A5-verification-framework.tex + A6 plain language
% Removed: appendix framing, Product 3 language
% Ozone categories → SPC categories
% Inlined plain-language explanations from A6
%----------------------------------------------------------

\section{Native Possibilistic Verification}\label{sec:native_verification}

The possibility-to-probability bridge
(Section~\ref{sec:poss_prob_bridge}) enables probabilistic verification
of possibilistic forecasts, but the conversion is lossy: it compresses
the full possibility shape into $n{+}1$ probabilities, with the entire
subnormality signal collapsed into a single ignorance bin.  Native
possibilistic verification evaluates the raw distribution directly,
preserving the shape information that the bridge discards.  The metrics
defined below complement the bridge-based lane by diagnosing forecast
quality in possibility space.

All definitions use the notation of Section~\ref{sec:poss_primer}:
$\pi$ denotes the raw (subnormal) possibility distribution,
$m = \max_{\omega \in \Omega} \pi(\omega)$ is the subnormality, and
$\ign = 1 - m$ is ignorance (Eq.~\ref{eq:ignorance}).

%----------------------------------------------------------
\subsection{Normalization Protocol}\label{sec:norm_protocol}

Some verification quantities require the raw distribution $\pi$ while
others require the normalized form $\pinorm = \pi / m$.  Two
normalization operations serve distinct purposes:

\begin{enumerate}[leftmargin=2em]
\item \textbf{Shape normalization} ($\pi \to \pinorm$): Rescales so
    that $\max(\pinorm) = 1$, isolating the system's relative
    preferences among outcomes.  Used for computing \Nc{}
    (Eq.~\ref{eq:cond_nec}) and the native scorecard
    (Section~\ref{sec:native_scorecard}).
\item \textbf{Possibility-to-probability bridge} ($\pi \to$ probability
    vector): Converts possibilities to probabilities with an explicit
    ignorance outcome (Section~\ref{sec:poss_prob_bridge}).  Used
    for information-gain verification.
\end{enumerate}

These are not alternatives: normalization operates within the possibility
space; the pignistic bridge converts to the probability space.

Table~\ref{tab:raw_norm} specifies which form each quantity uses.

\begin{table}[htbp]
\centering
\caption{Raw vs.\ normalized form used by each verification quantity.}
\label{tab:raw_norm}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}llcl@{}}
\toprule
Quantity & Symbol & Form & Reason \\
\midrule
\multicolumn{4}{@{}l}{\textit{Raw $(\pi)$}} \\[2pt]
\quad Ignorance              & $\ign$      & $\pi$    & Gap to normality \\
\quad Event possibility      & $\poss(A_T)$& $\pi$    & Upper bound \\
\quad Cumulative upper bound & $U_k$       & $\pi$    & For IRLS \\[4pt]
\multicolumn{4}{@{}l}{\textit{Normalized $(\pinorm)$}} \\[2pt]
\quad Depth-of-truth         & $\alpha^*$  & $\pinorm$ & Shape quality \\
\quad Nonspecificity          & $\eta$      & $\pinorm$ & Shape diffuseness \\
\quad Conditional necessity   & $\Nc(A)$    & $\pinorm$ & Certainty given coverage \\[4pt]
\multicolumn{4}{@{}l}{\textit{Both}} \\[2pt]
\quad Lower threshold bound  & $L_t$       & Both     & Coverage $\times$ certainty \\
\quad Cumulative lower bound & $L_k$       & Both     & IRLS counterpart \\
\bottomrule
\end{tabular}
\renewcommand{\arraystretch}{1.0}
\end{table}

\textbf{Why this matters:} Getting the raw/normalized distinction wrong
is the single most common error in possibilistic scoring.  Computing
\Nc{} from raw values yields nonsense (it would be negative when
$m < 0.5$).  Computing ignorance from normalized values gives zero by
definition.
The distinction is loosely analogous to evaluating ensemble-mean error
(which uses raw ensemble members) versus a rank histogram (which uses
the normalized rank position of the observation within the ensemble).

%----------------------------------------------------------
\subsection{Five-Number Scorecard}\label{sec:native_scorecard}

Five summary statistics evaluate each forecast--observation pair, where
$c_{\mathrm{obs}}$ denotes the observed SPC category and $K = |\Omega|$
is the number of categories (here, $K = 6$).
Table~\ref{tab:scorecard_defs} lists the definitions; the equations
are given below.

\begin{table}[htbp]
\centering
\caption{Five-number scorecard definitions.}
\label{tab:scorecard_defs}
\small
\begin{tabular}{clcl}
\toprule
Symbol & Name & Component & Eq.\ \\
\midrule
$\alpha^*$ & Depth-of-truth & Possibility
    & \eqref{eq:alpha_star} \\
$\eta$ & Nonspecificity & Possibility
    & \eqref{eq:ns} \\
$\delta$ & Resolution gap & Possibility
    & \eqref{eq:rg} \\
$\ign$ & Ignorance & Ignorance
    & \eqref{eq:ignorance} \\
$\Nc^*$ & Cond.\ necessity of truth & Necessity
    & \eqref{eq:nc_star} \\
\bottomrule
\end{tabular}
\end{table}

\begin{equation}
\alpha^* = \pinorm(c_{\mathrm{obs}}) \label{eq:alpha_star}
\end{equation}
where $\alpha^* = 1$ when the truth coincides with the system's peak
category and $\alpha^* = 0$ when the system assigns zero possibility to
the observed outcome.

Following \citet{Klir1995-va}, nonspecificity measures the diffuseness
of the normalized shape:
\begin{equation}
\eta = \frac{1}{K} \sum_{c=1}^{K} \pinorm(c) \label{eq:ns}
\end{equation}
ranging from $1/K$ (sharp) to $1$ (uniform).

\begin{equation}
\delta = \alpha^* - \eta \label{eq:rg}
\end{equation}
Positive $\delta$ indicates the system assigned more possibility to the
truth than to an average category.

\begin{equation}
\ign = 1 - m = 1 - \max_{\omega \in \Omega} \pi(\omega)
\end{equation}
(same as Eq.~\ref{eq:ignorance}).

\begin{equation}
\Nc^* = \Nc(c_{\mathrm{obs}})
    = 1 - \max_{\omega \neq c_{\mathrm{obs}}} \pinorm(\omega)
\label{eq:nc_star}
\end{equation}
where $\Nc^* > 0$ only when the observed category dominates all
alternatives in the normalized distribution.  This is a direct
application of Eq.~\eqref{eq:cond_nec} evaluated at the observation.

\medskip
\noindent\textbf{\textsf{\color{rationalecolor}Plain-language glosses}}
\smallskip

\noindent
\renewcommand{\arraystretch}{1.25}
\begin{tabularx}{\columnwidth}{%
    >{\columncolor{rationalecolor!10}}c X}
$\alpha^*$ & How much normalized possibility did the system assign to
    what actually happened?  Like POD for the winning bin, but
    continuous. \\
$\eta$ & How spread out was the normalized distribution?  Like ensemble
    spread or sharpness. \\
$\delta$ & Did the system give more support to the truth than to an
    average category?  Like resolution in the Brier decomposition. \\
$\ign$ & How much did the system admit it didn't know?
    $\ign = 0$: fully confident; $\ign = 1$: complete ignorance. \\
$\Nc^*$ & Among scenarios the system covered, was the truth dominant
    enough to rule out alternatives?  A confidence flag. \\
\end{tabularx}
\renewcommand{\arraystretch}{1.0}

Aggregates $\overline{\alpha^*}$, $\overline{\eta}$,
$\overline{\delta}$, $\overline{\ign}$, $\overline{\Nc^*}$ are reported
with bootstrap confidence intervals.

Figure~\ref{fig:performance_diagram} provides a diagnostic view of the
five scorecard metrics, inspired by \citet{Roebber2009-rv}.  Hexagonal
bins aggregate forecast--observation pairs by specificity $1 - \eta$
($x$-axis) and depth-of-truth $\alpha^*$ ($y$-axis).  Bin colour
encodes mean ignorance $\ign$: dark bins are confident, pale bins
uncertain.  Hyperbolic contours mark constant joint skill
$S = \alpha^* \times (1 - \eta)$; the dashed diagonal marks
$\delta = 0$.  The green trajectory connects category-conditional
means, tracing how the system degrades from \spc{NONE} (easy, upper
right) to \spc{HIGH} (rare, lower left), with point size proportional
to sample count.

The analogy with Roebber's diagram is deliberate: specificity plays the
role of success ratio (how focused was the distribution?), $\alpha^*$
plays the role of probability of detection (did the forecast capture
truth?), and the $S$ hyperbolas play the role of CSI contours (net
quality given both axes).  The key addition is ignorance $\ign$, encoded
as bin colour, which has no deterministic analogue.

\textit{Reading the diagram:} upper right is ideal (sharp and truthful);
lower right is the worst failure mode (sharp and wrong).  Above the
$\delta = 0$ diagonal, the forecast discriminated; below it, the truth
received less support than the categorical average.  The trajectory's
slope reveals where skill degrades fastest---a steep drop between
adjacent categories signals the system's breaking point.

\begin{figure*}[!t]
\centering
\includegraphics[width=0.5\textwidth]{performance_diagram.png}
\caption{Possibilistic performance diagram for a two-year synthetic
    reforecast.
    \textbf{Hex bins:} colour is mean $\ign$ (dark = confident).
    \textbf{Contours:} joint skill $S = \alpha^* \times (1 - \eta)$.
    \textbf{Dashed diagonal:} $\delta = 0$ break-even.
    \textbf{Green line:} category-conditional means from \spc{NONE}
    (easy, upper right) to \spc{HIGH} (rare, lower left); dot
    size $\propto$ sample count.
    \textbf{Stars:} worked scenarios from
    Section~\ref{sec:worked_examples} (green edge = hit, red = miss).
    The upper-right corner is the perfect-forecast region.}
\label{fig:performance_diagram}
\end{figure*}

Both axes of Figure~\ref{fig:performance_diagram} operate on the
normalised shape $\pi'$: two forecasts with identical shapes but
different commitment levels ($\ign = 0.05$ vs.\ $\ign = 0.50$) land at
the same position.  Commitment is visible only through bin colour---a
weaker perceptual channel.
Figure~\ref{fig:commitment_diagram} promotes commitment to a primary
axis: $x = \pi_{\max} = 1 - \ign$ (how much of the unit interval the
forecaster used) versus $y = \delta = \alpha^* - \eta$ (net
discrimination).  These axes are orthogonal---$\pi_{\max}$ is the raw
\emph{scale} of $\pi$; $\delta$ is the \emph{shape quality} of $\pi'$.

\textit{Reading the commitment diagram:} upper right is committed and
discriminating (ideal); lower right is overconfident and wrong; upper
left is the cautious forecast that got the shape right.  Below the
$\delta = 0$ line, the forecast failed to discriminate.  Anchor~C
(sharp-wrong) sits squarely in the overconfident quadrant; Anchor~B
(hedged-correct) sits in the cautious-discriminating region---a
distinction invisible in the shape-quality diagram.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{commitment_diagram.png}
\caption{Commitment--discrimination diagram for the same reforecast.
    \textbf{$x$:}~commitment $\pi_{\max} = 1 - \ign$ (how much the
    forecaster staked).
    \textbf{$y$:}~discrimination $\delta = \alpha^* - \eta$ (did the
    shape favour truth?).
    These axes are orthogonal: $x$ is the raw scale of $\pi$; $y$ is
    the quality of the normalised shape $\pi'$.
    \textbf{Hex bins:} coloured by count.
    \textbf{Green line:} category means from \spc{NONE} (upper right)
    to \spc{HIGH} (lower left).
    \textbf{Stars:} Section~\ref{sec:worked_examples} anchors.
    Upper right = committed \& discriminating.
    Lower right = overconfident \& wrong.
    Dashed line = $\delta = 0$ break-even.}
\label{fig:commitment_diagram}
\end{figure}

\FloatBarrier

%----------------------------------------------------------
\subsection{Interval Log Score}\label{sec:interval_log}

For a threshold event $A_T \subseteq \Omega$---any ``probability of
exceedance'' question, such as $A_T = \{\spc{ENH},\, \spc{MDT},\,
\spc{HIGH}\}$ for significant severe weather, or categories above
50\,ppb for ozone, or accumulations exceeding 5\,in.\ for
snowfall---the possibilistic forecast produces an interval $[L, U]$
rather than a point probability.  The bounds derive from the
three-component framework:

\begin{align}
U &= \poss(A_T) = \max_{\omega \in A_T} \pi(\omega)
    \label{eq:U_bound} \\
L &= m \cdot \Nc(A_T)
   = m \cdot \bigl[1 - \max_{\omega \notin A_T} \pinorm(\omega)\bigr]
    \label{eq:L_bound}
\end{align}

The \textit{interval log score} is:
\begin{equation}
\mathrm{ILS} = e \cdot \bigl[-\log_2\bigl(\max(U,\, \epsilon)\bigr)\bigr]
    + (1 - e) \cdot \bigl[-\log_2\bigl(\max(1 - L,\, \epsilon)\bigr)\bigr]
\label{eq:interval_log}
\end{equation}
where $e \in \{0, 1\}$ is the event indicator and $\epsilon$ is a small
positive floor (herein $\epsilon = 0.01$, capping the maximum penalty at
$-\log_2(0.01) \approx 6.64$~bits).  When $L = U = p$ (a point
probability), ILS reduces to the standard log score.

\textbf{Motivation.}
Logarithmic scoring is preferred over quadratic (Brier) scoring for rare
severe-weather events because the Brier score barely distinguishes a 0\%
forecast from a 5\% forecast when the event occurs: the squared
penalties are $1.0$ and $0.9025$, respectively.  The log score's
exponential penalty ($-\log_2 0.05 = 4.32$~bits vs.\ $-\log_2 0.0 \to
\infty$) more sharply penalizes overconfident misses, which is precisely
the failure mode of greatest operational concern.

\textbf{Three-component interpretation.}
The forecast assigns possibility $\poss = U$ to the threshold event,
conditional necessity $\Nc = L/m$, and ignorance $\ign = 1 - m$.  The
interval $[L, U] = [m \cdot \Nc,\; \poss]$ brackets the event's
plausibility; width $U - L$ reflects ambiguity between ``merely
possible'' and ``essentially certain.''

\textbf{Properties:}
\begin{itemize}[leftmargin=2em]
\item $0 \leq L \leq U \leq 1$ always holds.
\item Under normality ($m = 1$): $L = \nec(A_T)$, $U = \poss(A_T)$.
\item Under extreme subnormality ($m \to 0$): $U \to 0$, so ILS for an
    observed event approaches $-\log_2(\epsilon)$ (maximum penalty);
    $L \to 0$, so ILS for a non-event approaches $-\log_2(1) = 0$ (no
    penalty).  This correctly distinguishes ``no information and the
    event happened'' (costly) from ``no information and nothing
    happened'' (costless).
\end{itemize}

The ILS uses optimistic verification: when the event occurs, the penalty is $-\log_2(U)$, crediting the upper (most generous) bound rather than a point probability.  This means a forecaster can reduce the penalty by widening the interval---assigning higher possibility to more outcomes---without improving the forecast's sharpness.  The width penalty enters implicitly through $\ign$: widening the distribution increases subnormality, which the native scorecard's ignorance metric penalizes.  The ILS is therefore not strictly proper in the classical sense, but the companion $\ign$ metric partially mitigates gaming by exposing the cost of artificial interval inflation.

\textbf{Worked example} (Example~1 from Section~\ref{sec:subnormal},
$\pi = (0.05,\, 0,\, 0.1,\allowbreak\, 0.2,\, 0.75,\, 0.15)$, threshold
$A_T = \{\spc{ENH},\allowbreak\, \spc{MDT},\, \spc{HIGH}\}$):
\begin{align*}
m &= 0.75 \\
U &= \max(0.2,\, 0.75,\, 0.15) = 0.75 \\
\pinorm &= (0.067,\; 0.0,\; 0.133,\; 0.267,\; 1.0,\; 0.2) \\
\Nc(A_T) &= 1 - \max(0.067,\, 0.0,\, 0.133) = 0.867 \\
L &= 0.75 \times 0.867 = 0.65 \quad (L \leq U \;\checkmark) \\
\mathrm{ILS}_{\text{event}} &= -\log_2(0.75) = 0.42\;\text{bits} \\
\mathrm{ILS}_{\text{non-event}}
  &= -\log_2(1 - 0.65) = -\log_2(0.35) = 1.51\;\text{bits}
\end{align*}

Figure~\ref{fig:upper_lower} shows the interval $[L, U]$ for several
forecast scenarios applied to the threshold event
$A_T = \{\spc{ENH},\, \spc{MDT},\, \spc{HIGH}\}$, illustrating how
sharp forecasts produce narrow intervals while hedged or ignorant
forecasts produce wide ones.

\begin{figure*}[b]
\centering
\includegraphics[width=0.75\textwidth]{upper_lower_bounds.png}
\caption{Upper and lower probability bounds for the threshold event
    $A_T = \{\spc{ENH},\, \spc{MDT},\, \spc{HIGH}\}$ across three
    forecast scenarios.  Each horizontal band shows the interval
    $[L, U]$ (Eqs.~\ref{eq:L_bound}--\ref{eq:U_bound}) on a $[0,1]$
    number line.  Narrow intervals indicate high conditional necessity;
    wide intervals indicate ambiguity between ``merely possible'' and
    ``essentially certain.''}
\label{fig:upper_lower}
\end{figure*}

Two additional tools---the imprecise ranked logarithmic score (IRLS)
and possibilistic climatology---are developed in
Appendix~\ref{sec:appendix_alt}.  IRLS integrates the ILS across all
category boundaries; possibilistic climatology provides the consonant
baseline needed for skill scores.

\FloatBarrier
